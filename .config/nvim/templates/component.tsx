import { FC } from 'react';

interface {{_lua:require('textcase').api.to_pascal_case(vim.fn.expand('%:h:t'))_}}Props {
        
}

export const {{_lua:require('textcase').api.to_pascal_case(vim.fn.expand('%:h:t'))_}}: FC<{{_lua:require('textcase').api.to_pascal_case(vim.fn.expand('%:h:t'))_}}Props> = ({}) => {
        return <div>
        {{_cursor_}}
        </div>;
};
